

class Main {
    public static void main(String[] args) {
        int inputRange = 16;
         long prev = 0, current = 1;
        System.out.println(prev);
        if (inputRange > 1) System.out.println(current);
        for (int i = 2; i < inputRange; i++) {
            long next = prev + current;
            System.out.println(next);
            prev = current;
            current = next;
            
        } 
        
    }
}
